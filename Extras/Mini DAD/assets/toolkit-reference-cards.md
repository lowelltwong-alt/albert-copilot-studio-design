# Albert Team Toolkit 0.2 reference cards

These cards index the following useful 0.2.0 assets without copying their full
instructions into this standalone candidate package: `subagent-improver`,
`podcast-conversation-reviewer`, `subagent-improvement`, `learning-loop`,
`evidence-review`, `agent-routing`, `independent-verification`,
`repair-learning`, `subagent-review-fixtures`, and `conversational-podcast`.

They are discovery metadata for a human to locate and separately license/review
the source package. They are candidates, not automatically active guidance.
